# ابدأ من هنا — سياق مشروع Wanas على Shopify

ملف يقرأه أي شات جديد قبل ما يبدأ شغل. آخر تحديث: 16 أغسطس 2026.

> **ملاحظة لاحقة:** بعد آخر تحديث لهذا الملف اتشال `dashboard/` و`storefront/` و`web/` من الريبو نهائيًا. الجدول تحت ("النظام المبني") بيوصف حالة قديمة — الداشبورد والـ WhatsApp bot بقى مفيش داشبورد أصلًا، وواجهة الإدارة بقت Shopify Admin. للسياق الحالي الدقيق شوف `CLAUDE.md` و`README.md`.

---

## المشروع

**Wanas Gallery** — براند ستريت وير مصري. عندي نظام كامل مبني ومتقن في فولدر
`wanas`، والعميل طلب إن المتجر يبقى على **Shopify** بدل ما يفضل موقع مستقل.

### النظام المبني (شغال ومبسوط بيه)

| الجزء | المسار | الوصف |
|---|---|---|
| المتجر | `web/store/` | React (`index.html` + `app.js` + `ds/components.js`) |
| design tokens | `web/store/styles/tokens/` | 6 ملفات — ألوان، خطوط، مسافات، حواف، ظلال، حركة |
| الـ brief | `web/DESIGN-BRIEF.md` | مرجع التصميم |
| الـ API | `storefront/api.py` | FastAPI |
| الداتابيز | `wanas.db` | 18 منتج، 208 variant، 27 محافظة |
| لوحة الإدارة | `dashboard/` | بتقرا من `wanas.db` |
| WhatsApp bot | `chatbot/` | بيقرا من `wanas.db` |

### الكتالوج

18 منتج · 208 variant · **116 متاح بس** · الأسعار 200–800 جنيه · دفع كاش عند الباب.

6 تصنيفات بالترتيب ده (مهم — صف الكاتيجوريز معتمد عليه):

```
T-Shirts · Hoodies & Sweatshirts · Polo Shirts · Joggers & Sweatpants · Jackets · Tops
```

**تلات حاجات بتكسر أي تصميم ساذج** (من الـ brief):
1. الإتاحة **لكل تركيبة** مش لكل محور — الكايروكي تيشيرت فيه XL أسود ومفيهوش XL بني.
2. **السعر بيتغير مع اللون** — 3 منتجات. الهودي 650 أسود وزيتي، **700 رمادي**.
3. **14 من 18 منتج عليهم خصم** — فبادچ Sale على كل كارت بيفقد معناه.

---

## القرار المتفق عليه

**Shopify يبقى المصدر الرسمي للأوردرات والمخزون**، والـ dashboard والـ bot يتعدلوا
بعدين يقروا من Shopify Admin API بدل `wanas.db`.

**الـ checkout** ماشي بشكل Shopify متظبّط على هوية Wanas (التحكم الكامل فيه محتاج
باقة Plus). اللي بيضيع منه حاجة واحدة: لحظة "Is this you?" لما رقم التليفون يطابق
عميل موجود.

---

## المتجر

- الدومين: `p0hd05-m5.myshopify.com`
- **لسه مش launched**
- الفرع: Shebeen El-Kom
- التطبيق: `Chatbot Integration`، النسخة الشغالة `chatbot-integration-5`
- التوكن في `.env` تحت `SHOPIFY_ADMIN_TOKEN` (مش بيتشارك في الشات)
- الصلاحيات: منتجات + ثيمات + أوردرات + عملاء + **مخزون وlocations** (اتضافوا)

---

## الحالة: إيه اللي خلص

### ✅ الكتالوج — متأكد منه

**93 تصحيح اتطبقوا**، والنتيجة اتأكدت من الأدمن: **18 منتج و208 variant** مطابقين
للداتابيز.

اللي كان غلط واتصلح:
- المخزون كان 10 لكل variant بالتساوي → بقى حقيقي (116 من 208)
- الرمادي كان بسعر الأسود → بقى 700 في الهودي و800 في الزيب-هودي
- الـ Ringer كان 500 بخصم → بقى 580 من غير خصم في 3 ألوان (بقرار المستخدم)
- 10 تصنيفات غلط → اتصلحوا
- Worker Jacket كان 8 variants من غير Long/Short → بقى 16
- Zipup مكانش موجود → اتعمل بـ 12 variant و7 صور واتنشر على القناتين
- 3 أوصاف كان متحشور فيها نص أسعار → اتنضفوا

### ✅ الشات بوت — الأوردر كامل على Shopify

**لما البوت يبيع، بيتعمل أوردر حقيقي على Shopify** بـ `orderCreate` — بالعميل
والعنوان والشحن — وShopify هو اللي بيخصم المخزون. يعني طلبات الواتساب بقت ظاهرة
في شاشة Orders في الأدمن جنب طلبات الموقع.

- كل أوردر عليه تاجات **`chatbot` / `whatsapp` / `cash-on-delivery`** فتقدر تفلتر بيهم.
- **الرقم اللي العميل بيسمعه بقى رقم Shopify** (`#1004`). الـ `WNS-####` فضل مفتاح
  داخلي للداشبورد والأدوات. الأداة بترجّع الاتنين والبرومبت بيقول الـ `reference` بس.
- إيميل تأكيد Shopify **متمنوع** (`sendReceipt: false`) — البوت أصلًا بيبعت على واتساب.
- الإلغاء بيلغي أوردر Shopify بـ `restock` فالمخزون يرجع لوحده. تعديل الكمية والـ swap
  بيمشوا على `orderEditBegin/SetQuantity/Commit`.

⚠️ **الحجز اليدوي اتشال من مسار الأوردر عن قصد.** Shopify بيخصم مع `orderCreate`،
فلو حد رجّع `inventory.decrement` هنا المخزون هيتخصم **مرتين** وميبانش. الموجود
دلوقتي `record_sold` و`record_returned` — دول بيكتبوا في `wanas.db` بس.

مفيش transaction بين النظامين. الأوردر بيتعمل على Shopify **الأول** والمحلي بعده،
لأن أوردر على Shopify من غير صف محلي حاجة **باينة** في الأدمن، والعكس مخزون بيتباع
مرتين من غير ما حد ياخد باله. ولو الكتابة المحلية وقعت، أوردر Shopify بيتلغي.

3 حاجات في الكتابة تستاهل تتعرف:

1. **السعر اللي بيتسجل في الأوردر بقى سعر Shopify.** قبل كده كان البوت يقول للعميل
   السعر الجديد ويكتب في الفاتورة القديم — باج دخل مع تحويل القراءة واتصلح هنا.
2. **`compareQuantity`** مع كل خصم: Shopify نفسه بيرفض لو الرقم اتغير من ساعة ما
   قرينا. ده اللي بيمنع اتنين ياخدوا آخر قطعة، والمتجر داخل في الحساب مش الشات بس.
3. **لو Shopify مردّش، الأوردر بيترفض** بـ `store_unavailable`. والبرومبت متظبّط إن ده
   **مش** "خلصت" — العميل اللي يتقاله خلصت وهي موجودة بيروح يشتري من مكان تاني.

Shopify مالوش savepoint، فأي حجز لأوردر ماكملش بيترجع في `finally`.

| الملف الجديد | إيه فيه |
|---|---|
| `backend/services/shopify_orders.py` | `orderCreate` / `orderCancel` / order-edit |
| `backend/services/shopify_inventory.py` | حجز مباشر — لسه مستخدم لأوردرات ما قبل التحويل |
| `tests/fake_shopify.py` | Shopify وهمي شغال (مش mock) — رف + أوردرات |
| `tests/test_shopify_order_writes.py` | 14 تست للسعر والمخزون |
| `tests/test_shopify_full_orders.py` | 14 تست للأوردر الكامل — أهمهم منع الخصم المزدوج |

**314 تست عدّوا**، والمسار **اتجرّب على الستور الحقيقي**: أوردر `#1002` اتعمل،
الرصيد نزل 10→9، والإلغاء رجّعه 10. (`#1001` و`#1002` أوردرات فحص ملغية في
الأدمن — تقدر تمسحهم.)

`Order` اتضاف لها عمودين. الداتابيز الموجودة محتاجة:

```
python scripts/migrate_add_shopify_order_columns.py --apply
```

الأوردرات التلاتة القديمة هيفضلوا بـ NULL في العمودين، والكود بيتعامل معاهم
بالمسار القديم (الإلغاء يرجّع المخزون محليًا).

الفحص على الستور الحقيقي — بيعمل أوردر فعلي ويلغيه:

```
python scripts/shopify_check_live.py --write-test boxy-wns-tee-m-black \
                                     --order-test boxy-wns-tee-m-black
```

### تفاصيل Shopify API اتكلّفت وقت لحد ما ظبطت

عشان متضيعش وقت فيها تاني:

- الـ mutation هو **`inventorySetQuantities`** مش `inventoryAdjustQuantities`.
  `compareQuantity` مش موجود أصلًا على الـ adjust في نسخة 2025-01.
- الـ `reason` لازم من قايمة مقفولة. اللي بنستخدمه: **`reservation_created`**
  للحجز و**`reservation_deleted`** للإرجاع. `"sold"` مش قيمة صالحة.
- `ignoreCompareQuantity` لازم **False**. `shopify_sync.py` بيحطها True عن قصد
  لأنه تصحيح جماعي — متنقلهاش من هناك.
- `inventoryBehaviour` في `orderCreate` قيمه: `BYPASS` /
  `DECREMENT_IGNORING_POLICY` / **`DECREMENT_OBEYING_POLICY`** (اللي بنستخدمه).
  `DECREMENT_OBLIGATORY` **مش** موجود.
- **`orderCancel` غير متزامن.** بيرجّع `job`، والإلغاء والـ restock بيحصلوا بعدها
  بلحظات. رجوعها من غير خطأ معناه "Shopify وافق" مش "المخزون رجع". أي حاجة بتقرا
  الرف بعدها على طول هتشوف الرقم القديم — الفحص بيستنى 10 ثواني عشان كده.
- **التليفون لازم `+20...`.** `01067177128` بيترفض بـ "Phone is invalid" ويوقّع
  **الأوردر كله**. `normalise_phone` في `shopify_orders.py` بيحوّله، والرقم الأصلي
  بيتحط في الـ note كمان. الفحص بيبعت رقم بالصيغة المحلية عن قصد عشان يمسكها لو رجعت.

### ✅ القراءة

البوت بقى يقرا **السعر والمخزون** من Shopify لحظة ما العميل يسأل، بدل `wanas.db`.
باقي البيانات (style، department، collection، جداول المقاسات، صور الألوان) لسه من
`wanas.db` لأنها مالهاش مكان على Shopify أصلًا.

الربط بين الاتنين بقى بالـ **SKU** — كل variant على Shopify بيشيل الـ `variant_id`
بتاع `wanas.db`. المطابقة بالاسم اتشالت خالص من مسار البوت.

اللي اتعمل:

| الملف | إيه فيه |
|---|---|
| `backend/integrations/shopify_client.py` | GraphQL client للمسار الحي — بيرجّع `ShopifyUnavailable` بدل ما يوقع الطلب |
| `backend/services/shopify_catalog.py` | القراءة الحية + الـ snapshot اللي بيتاخد **مرة كل رسالة** مش كل tool call |
| `backend/services/catalog.py` | overlay: أرقام Shopify فوق صفوف `wanas.db` |
| `backend/services/inventory.py` | `available()` بقى حي، و`available_now()` لفحص لحظة الأوردر |
| `scripts/shopify_set_skus.py` | يكتب الـ SKUs — **لازم يتشغل الأول** |
| `scripts/shopify_check_live.py` | فحص، مبيكتبش حاجة |
| `tests/test_shopify_live_catalog.py` | 14 تست للسلوك الجديد |

**285 تست عدّوا.** لو Shopify مش موصول، البوت بيرجع لأرقام `wanas.db` وبيسجّل تحذير
مرة واحدة — مش بيقع.

**اتشغّل واتأكد:** الـ 208 SKUs اتكتبوا، والفحص رجّع `matched: 208` و
`Live read is working`، والطرفين متطابقين تمامًا. زمن أول نداء ~800ms.

`Zipup` كان ناقص من `PRODUCT_MAP` (اتعمل بعد ما الماب اتكتبت) — اتضاف، وده بيصلح
`shopify_sync.py` كمان.

`shopify_check_live.py` بيتشغل في أي وقت، مبيكتبش حاجة.

### ✅ الثيم — اترفع كـ draft

الـ 34 ملف اترفعوا على ثيم اسمه **`Wanas Gallery`** (`155353251996`) بحالة
unpublished. الثيم الشغّال متلمسش. المعاينة: Online Store ← Themes ← Preview.

اتصلح وقت الرفع:

- **`snippets/swatch.liquid` كان فيه غلط Liquid حقيقي** — مستخدم `when ... then`
  في سطر واحد. `then` مش من Liquid أصلًا (دي Ruby)، وجوة `{% liquid %}` كل تعليمة
  لازم سطر لوحدها. الملف اتكتب في الشات الأول ومااترفعش، فالغلط عاش من غير ما حد
  يشوفه. باقي الـ 33 ملف اتفحصوا — نضاف.
- **`shopify_finish.py` كان بيعمل ثيم جديد كل مرة** رغم إن الوصف بتاعه بيقول
  "safe to re-run". بقى بيدوّر على draft بنفس الاسم ويستبدل ملفاته. `--new-theme`
  لو عايز واحد جديد بالعافية.
- **بيتأكد من قواعد الـ collections** مش أساميها بس. `T-Shirts` و`Tops` طلعوا
  **يدويين** — والأولى فيها 7 منتجات بدل 5.

⚠️ **`T-Shirts` و`Tops` لسه محتاجين يتصلحوا.** Shopify مش بيسمح تحويل collection من
يدوية لأوتوماتيكية، فلازم يتمسحوا من الأدمن ويتعمل `--apply` تاني. من غير كده صف
الكاتيجوريز هيبان ناقص أو فيه منتجات غلط.

### الثيم — التفاصيل

`shopify-theme/` — 34 ملف. الـ 72 سطر توكنز **منقولين حرفيًا**.

فيه: hero بالدايرة الـ blush · صف الكاتيجوريز الدائري · كارت المنتج بالسعر كمدى
والسواتش · صفحة المنتج بالـ variant selector الكامل · صفحة القائمة بالشرايط والسورت ·
السلة · الفوتر · بانل المقاسات · بحث و404.

اللوجو **جوه الثيم** في `assets/logo.png` فمش محتاج رفع يدوي.

---

## الخطوة الجاية بالظبط

1. امسح `T-Shirts` و`Tops` من Products ← Collections (المنتجات نفسها مش بتتمسح).
2. `python scripts/shopify_finish.py --apply` — يعملهم بالقاعدة الصح.
3. عاين الثيم: Online Store ← Themes ← `Wanas Gallery` ← Preview.
4. وبعد ما يعجبك:

```
python scripts/shopify_finish.py --apply --publish-theme --skip-collections
```

بديل لو الـ API وقع: `wanas-shopify-theme.zip` جاهز للرفع اليدوي.

---

## السكريبتات

| السكريبت | بيعمل إيه |
|---|---|
| `scripts/shopify_sync.py` | يقارن الكتالوج بالداتابيز ويصلح الفروق. **يفضل مفيد كل ما المخزون يتحدث.** |
| `scripts/shopify_fix_structure.py` | Worker Jacket + Zipup |
| `scripts/shopify_finish.py` | collections + رفع الثيم |

كلهم **dry-run افتراضيًا**، محتاجين `--apply`، وكلهم idempotent.

---

## اللي لسه محتاج قرار

1. **الشحن** — Shopify عليه **118 جنيه لكل مصر**، والداتابيز بتقول **القاهرة 60
   والـ 26 محافظة التانية مفيش توصيل**. وكمان فيه منطقة **International** شغالة
   لـ 28 دولة رغم إن الدفع كاش عند الباب. **اتساب من غير قرار.**
2. **اسم المتجر** لسه `My Store` (إعدادات حساب).
3. **الـ dashboard** لسه بيقرا من `wanas.db`. الأوردرات بقت على Shopify كمان، بس
   الداشبورد لسه شايف النسخة المحلية.
4. **التوكن محتاج `write_orders`** — لو الفحص رفض عند القسم 6، دي السبب.
5. **العميل يقدر يتخطى قيود التوصيل**: يكتب عنوانه في المنوفية، البوت يرفض لأن
   مفيش سعر شحن، فيقول "خليها القاهرة" والبوت يوافق. الأوردر يطلع محافظته القاهرة
   بشحن 60 وعنوانه شبين الكوم. حصل فعلًا في `WNS-1003`. محتاج فحص يربط نص العنوان
   بالمحافظة المختارة — متعلق بقرار الشحن.
4. **12 size chart** محتاجين يتعملوا كصفحات Shopify (الـ snippet جاهز).

---

## ملاحظات للشات الجديد

- البيئة معزولة عن الإنترنت — **مقدرش أنادي Shopify API بنفسي**. السكريبتات بتتشغل
  على جهاز المستخدم.
- رفع الملفات عن طريق نوافذ الويندوز **مش متاح**. أي حاجة محتاجة file picker لازم
  تتحل بالـ API أو تتحط جوه الثيم.
- عندي وصول على Chrome وبقدر أشتغل في أدمن Shopify. الـ Dev Dashboard على
  `dev.shopify.com` مش `admin.shopify.com` (المسار القديم اتغير).
- ديالوجات Polaris معينة بتجمّد الـ screenshot — الحل إن في قايمة المنتجات فيه
  `... > Include in sales channels` وهي بتشتغل عادي.
- المستخدم بيتكلم مصري وعايز الردود مصري.
- **اتكلم بصراحة عن الحدود بدل ما تلفّها كقرار احترازي.**

---

## غلطات اتعملت في الشات الأول

عشان متتكررش:
- بدأت بتعديل ألوان على ثيم Horizon الجاهز — مكانش ده الطلب.
- قريت "E£ 30" من شاشة الأدمن وبنيت عليها إن كل الأسعار placeholder — غلط، كانت
  في معظمها مظبوطة. **اتحقق قبل ما تبني كلام على قراءة من شاشة.**
- وصفت مسار قديم للـ custom apps في Shopify.
- كتبت فحص صلاحيات مكانش بيختبر مسار الكتابة فعليًا.
