# Wanas على Shopify — ملخص التسليم والقرارات

كل اللي اتعمل من أول ما بدأنا، وكل قرار اتاخد من غير ما أرجعلك، وسببه.

---

## 1. الكتالوج — خلص ✅

**93 تصحيح اتطبقوا على المتجر**، والنتيجة اتأكدت من الأدمن: **18 منتج و208 variant**،
مطابقين لـ `wanas.db` بالظبط.

| | قبل | بعد |
|---|---|---|
| المنتجات | 17 | 18 |
| الـ variants | 188 | 208 |
| المخزون | 10 لكل variant بالتساوي | حقيقي (116 متاح من 208) |
| Worker Jacket | 8 variants، من غير Long/Short | 16 variant بالطولين |
| Zipup | مش موجود | موجود بـ 12 variant و7 صور |

**تصحيحات الأسعار:** الرمادي في الـ WANAS Hoodie بقى 700 بدل 650، وفي الـ Zip-Hoodie
800 بدل 750. والـ Ringer في البيچ والبني والنيڤي اترفع لـ 580 والخصم اتشال — ده كان
بقرارك.

**التصنيفات:** Worker Jacket بقى `Jackets`، والـ Knitted Polo و WANAS Polo بقوا
`Polo Shirts`، والسويت بانتس `Joggers & Sweatpants`. وتنضفت 3 أوصاف كان متحشور فيها
نص الأسعار.

### أدوات تفضل معاك

- `scripts/shopify_sync.py` — يقارن الكتالوج بالداتابيز ويصلح الفروق. شغّله من غير flag
  للمقارنة بس، وبـ `--apply` للتنفيذ. **هيفضل مفيد كل ما تحدّث المخزون في `wanas.db`.**
- `scripts/shopify_fix_structure.py` — الجاكيت والـ Zipup. idempotent، مش هيكرر شغله.

---

## 2. الثيم — اتبنى ✅

الملف **`wanas-shopify-theme.zip`** جاهز للرفع. الكود المصدري في `shopify-theme/`.

### إزاي ترفعه

`Online Store` ← `Themes` ← `Add theme` ← `Upload zip file` ← اختار الملف.

**هيترفع كـ draft مش هيتنشر.** ده قرار مقصود مني: مينفعش أنشر ثيم على متجر بيبيع
وانت نايم من غير ما تشوفه. اضغط `Preview`، وبعد ما تعجبك اضغط `Publish`.

### اللي اتبنى فيه

| الملف | إيه اللي فيه |
|---|---|
| `assets/tokens.css` | الـ 72 سطر توكنز **منقولين حرفيًا** من `web/store/styles/tokens/` |
| `assets/base.css` | كل الاستايلات، كلها بتحل لتوكنز — مفيش قيمة مخترعة |
| `assets/variant-picker.js` | منطق `computeVals()` كامل |
| `sections/hero.liquid` | الـ hero بالدايرة الـ blush ورا الميديا |
| `sections/category-row.liquid` | الصف الدائري، بيتسحب أفقيًا على الموبايل |
| `sections/main-product.liquid` | صفحة المنتج |
| `sections/main-collection.liquid` | القائمة بالشرايط والسورت |
| `sections/main-cart.liquid` | السلة |
| `snippets/product-card.liquid` | الكارت بالسعر كمدى والسواتش |
| `snippets/swatch.liquid` | خريطة الألوان الـ 12 |
| `snippets/size-chart.liquid` | بانل المقاسات |

### القواعد اللي في الـ brief واتنفذت

- **السعر بيتغير مع اللون.** الكارت بيعرض "from 650" لما فيه فرق، وصفحة المنتج
  بتحدّث السعر مع كل اختيار.
- **الـ sold out ظاهر ومشطوب مش مخفي.** `text-decoration: line-through` مش تخفيت
  رمادي، عشان يتقرا على موبايل في الشمس.
- **تغيير اللون بيعيد حساب المقاسات المتاحة.** ولو المقاس المختار بقى غير متاح
  بيتنقل لأقرب مقاس صالح بدل ما يفضل اختيار باطل.
- **البادچ الهادي.** 14 من 18 منتج عليهم خصم، فالـ Sale badge بقى cream هادي
  والـ Sold out بس هو اللي غامق.
- **الـ Collections شيلف مش مسار تصفح.** 8 منتجات مش في أي collection.

---

## 3. القرارات اللي اخدتها من غير ما أسألك

**الثيم يترفع draft مش published.** أخطر قرار في الليلة دي، واخدته بالاتجاه الآمن.

**الـ hero بيقبل صورة كبديل للفيديو.** الـ brief بيقول العميل جاي من إنستجرام على
موبايل وشبكة مش دايمًا كويسة. الفيديو شغال لو حطيته، بس البديل موجود.

**الأيقونات اتحطت inline بدل مكتبة lucide.** المتجر بيستخدم 5 أيقونات بس، وتحميل
مكتبة كاملة عشانهم تكلفة على موبايل.

**صف الكاتيجوريز بياخد collections مش product types.** Shopify بيتصفح بالـ collection.
عشان يطابق تصميمك بالظبط اعمل 6 automated collections شرطها "Product type يساوي"
بالترتيب: T-Shirts · Hoodies & Sweatshirts · Polo Shirts · Joggers & Sweatpants ·
Jackets · Tops. **دي أول حاجة تعملها لما تصحى** — من غيرها الصف هيبان فاضي.

**الـ Zipup اتنشر على القناتين** (Online Store و Point of Sale) زي باقي المنتجات،
ومحطّتوش في أي collection — لأن تعليق في `app.js` بيقول إن المحل مش عايز يعرضه كده.

**الشحن سبته زي ما هو** بناءً على كلامك. لسه فيه فرق: Shopify عليه **118 جنيه لكل
مصر**، والداتابيز بتقول **القاهرة 60 والباقي مفيش توصيل**. وفيه منطقة **International**
شغالة لـ 28 دولة رغم إن الدفع كاش عند الباب. **راجع دول قبل ما تنشر.**

**صفحة الـ checkout** ماشية بشكل Shopify متظبّط على الهوية، باتفاقنا.

---

## 4. أمر واحد يخلّص الباقي

```
python scripts/shopify_finish.py --apply
```

بيعمل تلات حاجات، وكلها إعادة تشغيلها آمنة:

1. **الـ 6 collections** بقاعدة "product type يساوي"، بالترتيب اللي صف الكاتيجوريز
   بيتوقعه. من غيرها الصف بيطلع فاضي.
2. **يرفع الثيم** كثيم **جديد draft**. مش بيلمس الثيم الشغال خالص.
3. بيسيبه draft عشان تعاينه.

ولما يعجبك:

```
python scripts/shopify_finish.py --apply --publish-theme --skip-collections
```

**اللوجو مبقاش محتاج رفع يدوي** — حطيته جوه الثيم في `assets/logo.png` والهيدر
بيستخدمه تلقائيًا. لو حطيت لوجو في theme settings بيتقدّم عليه.

لو الرفع بالـ API وقع لأي سبب، ملف `wanas-shopify-theme.zip` لسه موجود ومتطابق مع
نفس الملفات — ارفعه من `Online Store` ← `Themes` ← `Add theme` ← `Upload zip file`.

### اللي لسه محتاج قرار بشري

1. **الشحن** — Shopify عليه 118 لكل مصر، الداتابيز بتقول القاهرة 60 والباقي مفيش
   توصيل. وفيه International شغالة لـ 28 دولة مع إن الدفع كاش عند الباب.
2. **اسم المتجر** لسه `My Store` (إعدادات حساب).
3. **الـ dashboard والـ WhatsApp bot** لسه بيقروا من `wanas.db`.
4. **صفحات الـ size chart** — الـ snippet جاهز وبيقرا من صفحة Shopify، محتاج الـ 12
   شارت يتعملوا كصفحات.

---

## 5. حاجات غلطت فيها خلال الشغل

عشان تكون عارف تراجع إيه:

- بدأت بتعديل ألوان على ثيم Horizon الجاهز وده مكانش طلبك.
- قريت "E£ 30" من شاشة الأدمن وقلت إن كل الأسعار placeholder — وده كان غلط، الأسعار
  كانت في معظمها مظبوطة.
- وصفتلك مسار قديم في Shopify للـ custom apps، والواجهة اتغيرت لـ Dev Dashboard.
- كتبت فحص صلاحيات مكانش بيختبر مسار الكتابة، فالـ dry-run عدّى وهو مش شايف المشكلة.

الرقم الوحيد اللي أنا واثق فيه 100% هو مطابقة الكتالوج، لأني قارنته بالداتابيز
وبالأدمن. أي حاجة تانية تستاهل نظرة منك.
