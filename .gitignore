# Secrets
.env
.env.*
!.env.example

# Local databases
*.db
*.db-journal
*.db-wal
*.db-shm

# Photos customers send in. They land here so staff can see them in the
# handoff queue; they are not catalog data and must not be committed.
data/inbound/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
.claude/

# Test report artifacts written by ad-hoc pytest runs (not CI, which doesn't emit these)
.pyt.xml
.pytest-junit.xml

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Leftovers from the SFTP/FUSE mount: files deleted while still open. Not ours.
.fuse_hidden*
ziSuwgjf
